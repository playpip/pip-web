import { CoachReport } from '@/components/menu/CoachReport'

// Coaching across hands — the membership's half of the coaching line. The free
// per-hand read stays where it is, on the handover banner, and neither knows
// about the other (technology#56).
export default function Page() {
  return <CoachReport />
}
