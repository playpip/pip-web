/**
 * What the guides say about money, in one sentence.
 *
 * Six of them promised that no money was anywhere near the thing and that there
 * was none of it to spend; two said the game was simply free. Both are the
 * borrowed kind tests/priceClaims.test.ts was written about: true on the day,
 * false the day anything optional goes on sale. The membership has been on the
 * public roadmap since August, so they were already borrowed when this was
 * written. The phrasings themselves are in that test's list and deliberately
 * not repeated here, because this file is walked by it.
 *
 * The replacement says what cannot change instead of what happens to be in the
 * shop. "Free to play" is Will's own wording on the membership branch, in
 * `/terms` and on the landing page, so the site says it one way rather than
 * three. The chips being unsellable is a fact about how the game is built.
 *
 * One constant rather than eight sentences, for the reason the account sentence
 * in src/config/account.ts is one: within a week of the last change the site
 * was describing the same fact three different ways.
 */
export const FREE_TO_PLAY =
  'Pip is free to play, nothing to install, and the chips are play money that are not for sale.'
